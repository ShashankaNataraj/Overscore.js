import ArrayUtils from "../src/ArrayUtils";
describe("Suite", () => {
    describe("Array Utils", () => {
        it("should have a length", () => {
            expect(2).toEqual(2);
        });
    });
});